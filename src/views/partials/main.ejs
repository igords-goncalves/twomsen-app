<main class="w-full">
    <%- include('./form.ejs') %>
    <hr class="mt-8">
    <ul class="text-center text-lg mt-6">
        <% users.forEach(user => { %>
        <li class="list-none py-1"><strong><%= user.name %></strong>: <%= user.email %></li>
        <% }); %>
    </ul>
</main>
