<!DOCTYPE html>
<html>
<head>
  <title>Lista de Usuários</title>
</head>
<body>
  <h1>Lista de Usuários</h1>
  <ul>
    <% users.forEach(user => { %>
      <li><strong><%= user.name %></strong>: <%= user.email %></li>
    <% }); %>
  </ul>
</body>
</html>